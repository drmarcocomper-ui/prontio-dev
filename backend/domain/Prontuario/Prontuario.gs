/**
 * PRONTIO - Prontuario.gs
 * Mantido como “entry” do módulo por compatibilidade.
 * Implementação real do router em: domain/Prontuario/Prontuario.Router.gs
 */
function handleProntuarioAction(action, payload) {
  return handleProntuarioAction_(action, payload);
}
