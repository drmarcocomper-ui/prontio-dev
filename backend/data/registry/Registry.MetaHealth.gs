// backend/data/registry/Registry.MetaHealth.gs
/**
 * PRONTIO - Registry.MetaHealth.gs
 * Stub canônico (sem quebrar)
 *
 * Motivo:
 * - Registry.Core chama Registry_RegisterMetaHealth_(map)
 * - Se este arquivo estiver vazio, o build do Registry quebra
 *
 * Quando você implementar endpoints de health/meta,
 * substitua este stub registrando as actions reais.
 */

function Registry_RegisterMetaHealth_(map) {
  // Stub: não registra actions por enquanto.
  return;
}
